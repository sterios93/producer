import { set, toggle } from '@/utils/vuex'

const state = () => ({
  id: 0,
  items: [],
  discount: 10,
  active: true,
  schedule: null,
  timeStart: null,
  timeEnd: null
})

export default {
  state,
  mutations: {},
  getters: {},
  actions: {
  }
}


