import { set, toggle } from '@/utils/vuex'

const state = () => ({

})

export default {
  state,
  mutations: {},
  getters: {},
  actions: {
    saveItem({rootState, state, commit}, {action, payload}) {
      return undefined;
    }
  }
}


